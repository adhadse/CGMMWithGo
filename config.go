package main

// Defining xMax, yMax and xMin, yMin for
// clipping rectangle (check clipping.go).
// This is also used for application window's Height and Depth
const (
	xMin = 0
	yMin = 0
	xMax = 800
	yMax = 600
)

